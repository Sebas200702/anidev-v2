/**
 * Public exports for the toolbar island.
 *
 * @module shared/components/toolbar
 * @remarks
 * React (CSR) component — needs a `client:*` directive at the usage site.
 *
 * @see {@link Toolbar}
 */

export { Toolbar } from './toolbar'
