/**
 * Public exports for the shared button component.
 *
 * @module shared/components/button
 *
 * @see {@link Button} — the component
 * @see {@link ButtonProps} — its props
 */

export { default as Button } from './button.astro'
export type { ButtonProps, ButtonVariant } from './types'
