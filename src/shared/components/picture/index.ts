/**
 * Responsive `<picture>` Astro component and related exports.
 *
 * @module shared/components/picture
 * @remarks
 * Re-exports the `picture.astro` component for use in pages and domain-specific layouts.
 * The component handles srcset/sizes and fallback sources for optimized media delivery.
 *
 * @see `./picture.astro` — implementation (Astro SFC, not TypeScript)
 */

export * from './picture'
