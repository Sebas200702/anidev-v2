/**
 * Public exports for the airing-status color helpers.
 *
 * @module shared/utils/get-status-color
 *
 * @see {@link getStatusColor} — token name for a status
 * @see {@link getStatusDotClass} — ready-to-use background class
 */

export { getStatusColor, getStatusDotClass } from './util'
