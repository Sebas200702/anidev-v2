/**
 * Shared Astro layout components for consistent page chrome across the app.
 *
 * @module shared/layouts
 * @remarks
 * Re-exports {@link base-layout} (base-layout.astro) providing HTML shell, meta slots, and global styles
 * used by domain pages.
 *
 * @see `./base-layout.astro` — base HTML document wrapper
 */

export * from './base-layout'
