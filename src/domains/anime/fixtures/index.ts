/**
 * Bootstrap fixtures for the anime domain's showcase entries.
 *
 * @module domains/anime/fixtures
 * @remarks
 * The showcase route falls back to these when a service yields no record.
 * Components never import them.
 *
 * @see {@link module:domains/anime/showcase/entries}
 */

export { animeCardFixture } from './anime-card'
export { animeDetailsFixture } from './anime-details'
export { carouselItemFixture } from './carousel-item'
