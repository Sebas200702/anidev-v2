/**
 * @module @media/schemas
 * @remarks Barrel exports for media route validation schemas.
 * @see {@link ./media-schema} for the media proxy request schema
 * @example
 * ```typescript
 * import { mediaRequestSchema } from '@media/schemas'
 * ```
 */

export { mediaRequestSchema } from './media-schema'
