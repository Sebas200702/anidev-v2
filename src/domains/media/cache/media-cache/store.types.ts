/**
 * @module domains/media/cache/media-cache/store.types
 * @remarks Shape of raw metadata payloads stored alongside serialized media in the cache
 * backend.
 */

export interface RawMeta {
  src: string
}
